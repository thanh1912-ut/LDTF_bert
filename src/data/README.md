# Thư mục dữ liệu cũ

Các tài liệu và pipeline dữ liệu đang sử dụng đã được chuyển sang:

```text
src/pipeline data processed/
```

Tài liệu chính thức của phiên bản dữ liệu đa nguồn nằm tại:

```text
docs/data/
```

Không thêm script hoặc kế hoạch xử lý dữ liệu mới vào thư mục này.
